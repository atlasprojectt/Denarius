// Vitest stub: `server-only` is a build-time guard for Next.js; in node tests
// the guarded modules (crypto, sync, queries) are exactly where the logic
// under test lives, so the marker resolves to this empty module.
export {};
