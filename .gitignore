# Local Claude Code settings
.claude/settings.local.json

# Node / Next.js (para o build que vem)
node_modules/
.pnpm-store/
.next/
out/
build/
# Secrets: ignore every .env* file, but keep the committed template
.env*
!.env.example
.vercel/

# OS
.DS_Store
Thumbs.db

# TS build cache
*.tsbuildinfo
.vercel

# Playwright artifacts (also keeps Tailwind v4 source-scan away from HTML dumps)
test-results/
playwright-report/

# Local agent-tool scratch (dev-server logs only — nothing reproducible)
.codex/
