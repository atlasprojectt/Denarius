# Local Claude Code settings
.claude/settings.local.json

# Node / Next.js (para o build que vem)
node_modules/
.pnpm-store/
.next/
out/
build/
# Secrets: ignore every .env* file, but keep the committed template
.env*
!.env.example
.vercel/

# OS
.DS_Store
Thumbs.db

# TS build cache
*.tsbuildinfo
.vercel

# Playwright artifacts (also keeps Tailwind v4 source-scan away from HTML dumps)
test-results/
playwright-report/

# Local development server output
.denarius-dev.*.log

# Local agent skills — vendored, never commit (see docs/skills.md).
# Skills live in .agents/skills/ (project scope) and stay on this machine.
.agents/skills/how/
.agents/skills/blast-radius/
.agents/skills/typescript-best-practices/
.agents/skills/improve-codebase-architecture/
.agents/skills/to-tickets/
.agents/skills/tdd/
.agents/skills/code-review/
.agents/skills/handoff/

# Local ticket files published by the to-tickets skill
.scratch/
